"use client";

/**
 * ModeProvider: placeholder for applying system modes to the document (e.g. dark class on html).
 */
export function ModeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
